name: Unity Game Test

on: [push, pull_request]

jobs:
  test-game:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Install dependencies
      run: sudo apt-get install -y p7zip-full xvfb xdotool ffmpeg

    - name: Extract the game archive
      run: 7z x apk/Shadow_Ops.7z.001 -oapk/game

    - name: Start virtual display
      run: |
        Xvfb :99 -screen 0 1280x720x24 &
        echo "DISPLAY=:99" >> $GITHUB_ENV

    - name: Run game with virtual display
      run: |
        chmod +x apk/game/Shadow_Ops.x86_64
        DISPLAY=:99 xvfb-run --auto-servernum apk/game/Shadow_Ops.x86_64 &
        sleep 5  # Wait for the game to load

    - name: Simulate Input
      run: |
        DISPLAY=:99 xdotool key space

    - name: Start screen recording
      run: |
        ffmpeg -y -f x11grab -video_size 1280x720 -i :99 -t 30 -r 30 -pix_fmt yuv420p game_play.mp4 &

    - name: Upload video
      uses: actions/upload-artifact@v4
      with:
        name: game-play-video
        path: game_play.mp4
